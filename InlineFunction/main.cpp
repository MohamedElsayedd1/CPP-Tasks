#include <iostream>
#include <string>
#include <cctype>
#include <cstring>
#include <vector>

using namespace std;

inline add_numbers(int x, int y){
    return x + y;
}

int main()
{
    int sum {0};
    sum = add_numbers(10, 40);
    cout << "sum = " << sum << endl;
}
